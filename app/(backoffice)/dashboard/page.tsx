"use client";

import { receivedAmount } from "@/lib/billing-utils";
import Link from "next/link";
import { useMemo } from "react";
import { PageHead } from "@/components/page-head";
import { ApiNotice } from "@/components/api-notice";
import { useBranch } from "@/components/branch-context";
import { useApiQuery } from "@/lib/use-api";
import type { ApiRoom, InvoiceDto, PropertyDto } from "@/lib/api-types";

const noProperties: PropertyDto[] = [];
const noInvoices: InvoiceDto[] = [];
const roomLabel: Record<string, string> = { VACANT: "ว่าง", OCCUPIED: "มีผู้เช่า", RESERVED: "จองแล้ว", MAINTENANCE: "ซ่อมบำรุง" };
const invoiceStatusLabel: Record<string, string> = { ISSUED: "ออกบิลแล้ว", PENDING_REVIEW: "รอตรวจสลิป", PARTIALLY_PAID: "ชำระบางส่วน", PAID: "ชำระแล้ว", OVERDUE: "เกินกำหนด" };
const money = (value: number) => `฿${value.toLocaleString("th-TH", { maximumFractionDigits: 2 })}`;
const monthName = (month: number) => new Intl.DateTimeFormat("th-TH", { month: "short" }).format(new Date(2026, month - 1, 1));

type RoomInvoice = ApiRoom & { building: string; invoice?: InvoiceDto };

export default function Dashboard() {
  const { selectedBranch, selectedBranchId, loading: branchesLoading } = useBranch();
  const properties = useApiQuery(selectedBranchId ? `/branches/${selectedBranchId}/properties` : null, noProperties);
  const invoices = useApiQuery(selectedBranchId ? `/branches/${selectedBranchId}/invoices` : null, noInvoices);
  const rooms = useMemo(() => properties.data.flatMap((property) => property.buildings.flatMap((building) => building.rooms.map((room) => ({ ...room, building: building.name })))), [properties.data]);
  const currentPeriod = useMemo(() => { const now = new Date(); return { year: now.getFullYear(), month: now.getMonth() + 1 }; }, []);
  const currentInvoices = useMemo(() => invoices.data.filter((invoice) => invoice.period?.year === currentPeriod.year && invoice.period.month === currentPeriod.month && invoice.status !== "VOID"), [currentPeriod, invoices.data]);
  const roomInvoices = useMemo<RoomInvoice[]>(() => rooms.map((room) => ({ ...room, invoice: currentInvoices.find((invoice) => invoice.room.id === room.id) })), [currentInvoices, rooms]);
  const stats = useMemo(() => { const occupied = rooms.filter((room) => room.status === "OCCUPIED").length; const issued = invoices.data.filter((invoice) => !["DRAFT", "VOID"].includes(invoice.status)); const billed = issued.reduce((sum, invoice) => sum + Number(invoice.total), 0); const received = issued.reduce((sum, invoice) => sum + receivedAmount(invoice), 0); return { occupied, billed, received, outstanding: issued.reduce((sum, invoice) => sum + Math.max(0, Number(invoice.total) - receivedAmount(invoice)), 0) }; }, [invoices.data, rooms]);
  const trend = useMemo(() => { const now = new Date(); return Array.from({ length: 6 }, (_, index) => { const date = new Date(now.getFullYear(), now.getMonth() - (5 - index), 1); const year = date.getFullYear(); const month = date.getMonth() + 1; const bills = invoices.data.filter((invoice) => invoice.period?.year === year && invoice.period.month === month && !["VOID", "DRAFT"].includes(invoice.status)); return { label: monthName(month), billed: bills.reduce((sum, bill) => sum + Number(bill.total), 0), received: bills.reduce((sum, bill) => sum + receivedAmount(bill), 0) }; }); }, [invoices.data]);
  const tasks = { missing: roomInvoices.filter((room) => room.status === "OCCUPIED" && !room.invoice).length, drafts: currentInvoices.filter((invoice) => invoice.status === "DRAFT").length, pending: invoices.data.reduce((sum, invoice) => sum + (invoice.payments ?? []).filter((payment) => payment.status === "PENDING").length, 0), overdue: invoices.data.filter((invoice) => !["DRAFT", "VOID", "PAID"].includes(invoice.status) && new Date(invoice.dueDate) < new Date() && Number(invoice.total) > receivedAmount(invoice)).length };
  const trendMax = Math.max(1, ...trend.map((item) => Math.max(item.billed, item.received)));
  const loading = branchesLoading || properties.loading || invoices.loading;
  const error = properties.error || invoices.error;

  return <>
    <PageHead eyebrow="ห้องบัญชี" title={selectedBranch ? `ภาพรวม${selectedBranch.name}` : "ภาพรวม"} subtitle={selectedBranch ? "ดูห้องพักและสถานะใบแจ้งหนี้ประจำเดือนแบบภาพเดียว" : "สร้างสาขาและเพิ่มห้องก่อน ระบบจะแสดงข้อมูลที่นี่"} />
    <ApiNotice loading={loading} error={error} />
    {!loading && !selectedBranch && <section className="empty-state">ยังไม่มีสาขา ให้เริ่มจากเมนู “ร้านและสาขา”</section>}
    {selectedBranch && <>
      <section className="panel" aria-label="งานที่ต้องทำ"><div className="panel-head"><div><h2>งานที่ต้องทำ</h2><p>เลือกงานเพื่อดำเนินการต่อ</p></div></div><div className="task-grid"><Link href="/bills"><strong>{tasks.missing}</strong><span>ห้องรอสร้างบิลเดือนนี้ →</span></Link><Link href="/bills?view=done"><strong>{tasks.drafts}</strong><span>บิลร่างรอออกเดือนนี้ →</span></Link><Link href="/payments"><strong>{tasks.pending}</strong><span>สลิปรอตรวจ →</span></Link><Link href="/calendar"><strong>{tasks.overdue}</strong><span>บิลเกินกำหนด →</span></Link></div></section>
      <section className="summary-strip" aria-label="ตัวเลขสำคัญ"><div className="metric"><small>อัตราเข้าพัก</small><strong className="mono">{rooms.length ? `${((stats.occupied / rooms.length) * 100).toFixed(1)}%` : "—"}</strong><span>{stats.occupied} / {rooms.length} ห้อง</span></div><div className="metric"><small>ยอดเรียกเก็บ</small><strong className="mono">{money(stats.billed)}</strong><span>สะสมทุกรอบบิลที่ออกแล้ว</span></div><div className="metric"><small>รับชำระแล้ว</small><strong className="mono">{money(stats.received)}</strong><span>รวมยอดอนุมัติที่ชำระบางส่วน</span></div><div className="metric"><small>ค้างชำระ</small><strong className="mono" style={{ color:"var(--coral)" }}>{money(stats.outstanding)}</strong><span>ยอดคงเหลือหลังหักรับชำระ</span></div></section>
      <div className="dashboard-grid"><section className="panel trend-panel"><div className="panel-head"><div><h2>แนวโน้มการชำระเงิน</h2><p>ย้อนหลัง 6 เดือน · ยอดเรียกเก็บเทียบกับรับชำระสะสมตามรอบบิล</p></div><div className="chart-legend"><span><i className="legend-dot billed" />เรียกเก็บ</span><span><i className="legend-dot received" />รับแล้ว</span></div></div><div className="revenue-chart"><div className="chart-scale" aria-hidden="true">{[1,.75,.5,.25,0].map(ratio=><span key={ratio}>{new Intl.NumberFormat("th-TH", {notation:"compact", maximumFractionDigits:1}).format(trendMax * ratio)}</span>)}</div><div className="trend-chart" role="img" aria-label="ยอดเรียกเก็บสีส้มและยอดรับชำระสีเขียว ย้อนหลัง 6 เดือน ดูยอดละเอียดในตารางใต้กราฟ">{trend.map((item,index) => <div className="trend-column" key={index}><div className="trend-bars"><i className="bar billed" style={{ height: `${item.billed / trendMax * 100}%` }} /><i className="bar received" style={{ height: `${item.received / trendMax * 100}%` }} /></div><span>{item.label}</span></div>)}</div></div><details className="chart-details"><summary>ดูยอดเงินรายเดือน</summary><div className="table-wrap"><table className="data-table"><caption className="sr-only">ยอดเงินย้อนหลัง 6 เดือน หน่วยบาท</caption><thead><tr><th>เดือน</th><th>เรียกเก็บ</th><th>รับชำระแล้ว</th></tr></thead><tbody>{trend.map((item,index)=><tr key={index}><td>{item.label}</td><td>{money(item.billed)}</td><td>{money(item.received)}</td></tr>)}</tbody></table></div></details></section><section className="panel occupancy-panel"><div className="panel-head"><div><h2>สถานะห้องพัก</h2><p>{rooms.length} ห้องในสาขานี้</p></div></div>{rooms.length ? <div className="occupancy-content"><div className="occupancy-ring" style={{background: `conic-gradient(${["OCCUPIED","VACANT","RESERVED","MAINTENANCE"].map((status,index,all)=>{const before=all.slice(0,index).reduce((n,key)=>n+rooms.filter(r=>r.status===key).length,0)/rooms.length*100;const after=before+rooms.filter(r=>r.status===status).length/rooms.length*100;return `var(--room-${status.toLowerCase()}) ${before}% ${after}%`}).join(",")})`}}><div><strong>{stats.occupied}</strong><span>ห้องมีผู้เช่า</span></div></div><div className="occupancy-legend">{Object.entries(roomLabel).map(([status,label])=><div key={status}><span><i style={{background:`var(--room-${status.toLowerCase()})`}}/>{label}</span><b>{rooms.filter(room=>room.status===status).length} ห้อง</b></div>)}</div></div> : <p className="empty-state">เพิ่มห้องพักเพื่อดูสัดส่วนการเข้าพัก</p>}</section>
      <section className="panel room-ledger-panel"><div className="room-ledger-head"><div><p className="eyebrow">ผังห้องประจำเดือน</p><h2>ใบแจ้งหนี้เดือนนี้</h2><p>ดูสถานะรายห้องแบบเดียวกับผังที่นั่ง — {monthName(currentPeriod.month)} {currentPeriod.year + 543}</p></div><div className="room-ledger-legend"><span><i className="seat-dot no-bill" />ยังไม่ออกบิล</span><span><i className="seat-dot billed" />ออกบิลแล้ว</span><span><i className="seat-dot paid" />ชำระแล้ว</span></div></div>{roomInvoices.length ? <div className="room-seat-grid">{roomInvoices.map((room) => { const invoice = room.invoice; const status = invoice?.status ?? "NO_BILL"; return <article className={`room-seat-card ${status.toLowerCase()}`} key={room.id}><div className="room-seat-top"><span className="room-seat-number">{room.number}</span><span className="room-seat-building">{room.building}</span></div><div className="room-seat-customer">{invoice ? <><strong>{invoice.contract.resident.fullName}</strong><small>{invoice.number}</small></> : <><strong className="no-resident">ยังไม่มีใบแจ้งหนี้</strong><small>{room.status === "OCCUPIED" ? "มีผู้เช่า · รอดำเนินการ" : roomLabel[room.status]}</small></>}</div><div className="room-seat-bottom"><span className="invoice-status-dot"><i />{invoiceStatusLabel[status] ?? "ยังไม่ออกบิล"}</span>{invoice && <b>{money(Number(invoice.total))}</b>}</div></article>; })}</div> : <p className="empty-state">ยังไม่มีห้องพักสำหรับแสดงผัง</p>}</section></div></>}
  </>;
}
